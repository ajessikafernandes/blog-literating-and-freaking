
info:
  app:
    name: literating-and-freaking-out
    description: Blog sobre livros e resenhas, com foco em Terror, Sci-fi, Fantasia, Distopia, HQ e Clássicos.
    version: v0.0.1

server:
  port: 4200

#spring:
#  datasource:
#    url: jdbc:postgresql://localhost:5432/literatingandfreakingout
#    username: postgres
#    password: pOS%EGr3s
#    driver-class-name: org.postgresql.Driver
#  jpa:
#    show-sql: true
#    hibernate:
#      ddl-auto: update
#    database-platform: org.hibernate.dialect.PostgreSQLDialect
#  properties:
#    hibernate:
#      jdbc:
#        lob:
#          nonon_contextual_creation: trueblog

spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:blog
    username: ADMINAPIBLOG
    password: PASSW0RD
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.H2Dialect